apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-html
  namespace: nginx-gce
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <meta charset="UTF-8">
        <title>NGINX ALB Demo - v${NGINX_VERSION}</title>
        <style>
            body { font-family: Arial; margin: 40px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; }
            .container { background: rgba(255,255,255,0.1); padding: 30px; border-radius: 10px; backdrop-filter: blur(10px); }
            h1 { font-size: 2.5em; margin-bottom: 20px; }
            .version { font-size: 3em; font-weight: bold; color: #ffd700; text-shadow: 2px 2px 4px rgba(0,0,0,0.5); }
            .badge { background: rgba(255,215,0,0.2); padding: 10px 20px; border-radius: 5px; display: inline-block; margin: 20px 0; }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>🚀 NGINX with ALB</h1>
            <div class="badge">
                <div class="version">📦 Version ${NGINX_VERSION}</div>
            </div>
            <p>This NGINX instance is served through an AWS Application Load Balancer!</p>
            <p>Features:</p>
            <ul>
                <li>3 replicas for high availability</li>
                <li>ALB with internet-facing scheme</li>
                <li>Health checks enabled</li>
                <li>Rolling updates with zero downtime</li>
                <li>🎯 Deployed via ArgoCD GitOps</li>
            </ul>
            <p style="margin-top: 30px; font-size: 0.9em; opacity: 0.8;">
                ⚙️ Deployed from: overlays/dev | Image: nginx:${NGINX_VERSION}
            </p>
        </div>
    </body>
    </html>
