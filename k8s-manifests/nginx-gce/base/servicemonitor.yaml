apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-metrics
  namespace: nginx-gce
  labels:
    app: nginx-gce
spec:
  selector:
    matchLabels:
      app: nginx-gce
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
