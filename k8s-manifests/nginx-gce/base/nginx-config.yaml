apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: nginx-gce
data:
  nginx.conf: |
    events {
        worker_connections 1024;
    }

    http {
        include       /etc/nginx/mime.types;
        default_type  application/octet-stream;

        log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                          '$status $body_bytes_sent "$http_referer" '
                          '"$http_user_agent" "$http_x_forwarded_for"';

        access_log  /var/log/nginx/access.log  main;
        error_log   /var/log/nginx/error.log warn;

        sendfile        on;
        keepalive_timeout  65;

        server {
            listen 80;
            server_name _;

            location / {
                root   /usr/share/nginx/html;
                index  index.html index.htm;
            }

            # Proxy API requests to backend-api service (with Linkerd mTLS)
            location /api/ {
                proxy_pass http://backend-api.backend-api.svc.cluster.local:8080;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
                proxy_connect_timeout 5s;
                proxy_send_timeout 60s;
                proxy_read_timeout 60s;
            }

            # Stub status endpoint for prometheus exporter
            location /nginx_status {
                stub_status on;
                access_log off;
                allow 127.0.0.1;  # Only allow localhost (sidecar)
                deny all;
            }
        }
    }
